# Comparing `tmp/opentf_toolkit-0.45.0-py3-none-any.whl.zip` & `tmp/opentf_toolkit-0.46.0-py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,46 +1,47 @@
-Zip file size: 66936 bytes, number of entries: 44
--rw-r--r--  2.0 unx    40134 b- defN 23-Mar-28 12:52 opentf/commons/__init__.py
--rw-r--r--  2.0 unx    14520 b- defN 23-Mar-28 12:52 opentf/commons/expressions.py
--rw-r--r--  2.0 unx        0 b- defN 23-Mar-28 12:52 opentf/schemas/__init__.py
--rw-rw-rw-  2.0 unx     2105 b- defN 23-Mar-28 12:52 opentf/schemas/abac.opentestfactory.org/v1alpha1/Policy.json
--rw-rw-rw-  2.0 unx     1230 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/AgentRegistration.json
--rw-rw-rw-  2.0 unx     1366 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/AllureCollectorOutput.json
--rw-rw-rw-  2.0 unx     2174 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/EventBusConfig.json
--rw-rw-rw-  2.0 unx     2702 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/ExecutionCommand.json
--rw-rw-rw-  2.0 unx     1143 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/ExecutionError.json
--rw-rw-rw-  2.0 unx     2226 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/ExecutionResult.json
--rw-rw-rw-  2.0 unx     1356 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/GeneratorCommand.json
--rw-rw-rw-  2.0 unx     6633 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/GeneratorResult.json
--rw-rw-rw-  2.0 unx     2219 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/Notification.json
--rw-rw-rw-  2.0 unx     2803 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/PluginMetadata.json
--rw-rw-rw-  2.0 unx     2875 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/ProviderCommand.json
--rw-rw-rw-  2.0 unx     3878 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/ProviderConfig.json
--rw-rw-rw-  2.0 unx     4259 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/ProviderResult.json
--rw-rw-rw-  2.0 unx     6301 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/SSHServiceConfig.json
--rw-rw-rw-  2.0 unx     2835 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/ServiceConfig.json
--rw-rw-rw-  2.0 unx     2415 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/Subscription.json
--rw-rw-rw-  2.0 unx     9899 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/Workflow.json
--rw-rw-rw-  2.0 unx     1149 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowCanceled.json
--rw-rw-rw-  2.0 unx     1110 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowCompleted.json
--rw-rw-rw-  2.0 unx     1264 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowResult.json
--rw-rw-rw-  2.0 unx     2232 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha2/Notification.json
--rw-rw-rw-  2.0 unx     6445 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1alpha2/SSHServiceConfig.json
--rw-rw-rw-  2.0 unx     3492 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1beta1/ExecutionCommand.json
--rw-rw-rw-  2.0 unx    10205 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1beta1/GeneratorResult.json
--rw-rw-rw-  2.0 unx     4535 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1beta1/ProviderCommand.json
--rw-rw-rw-  2.0 unx     4563 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1beta1/ProviderConfig.json
--rw-rw-rw-  2.0 unx     5050 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1beta1/ProviderResult.json
--rw-rw-rw-  2.0 unx     2850 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1beta1/ServiceConfig.json
--rw-rw-rw-  2.0 unx    15369 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1beta1/Workflow.json
--rw-rw-rw-  2.0 unx     3517 b- defN 23-Mar-28 12:52 opentf/schemas/opentestfactory.org/v1beta2/ServiceConfig.json
--rw-rw-rw-  2.0 unx     1579 b- defN 23-Mar-28 12:52 opentf/scripts/launch_java_service.sh
--rw-r--r--  2.0 unx    19009 b- defN 23-Mar-28 12:52 opentf/scripts/startup.py
--rw-r--r--  2.0 unx    13537 b- defN 23-Mar-28 12:52 opentf/toolkit/__init__.py
--rw-r--r--  2.0 unx    13130 b- defN 23-Mar-28 12:52 opentf/toolkit/channels.py
--rw-r--r--  2.0 unx     7164 b- defN 23-Mar-28 12:52 opentf/toolkit/core.py
--rw-rw-rw-  2.0 unx    11357 b- defN 23-Mar-28 12:52 opentf_toolkit-0.45.0.dist-info/LICENSE
--rw-r--r--  2.0 unx     1909 b- defN 23-Mar-28 12:52 opentf_toolkit-0.45.0.dist-info/METADATA
--rw-r--r--  2.0 unx       92 b- defN 23-Mar-28 12:52 opentf_toolkit-0.45.0.dist-info/WHEEL
--rw-r--r--  2.0 unx        7 b- defN 23-Mar-28 12:52 opentf_toolkit-0.45.0.dist-info/top_level.txt
--rw-rw-r--  2.0 unx     4821 b- defN 23-Mar-28 12:52 opentf_toolkit-0.45.0.dist-info/RECORD
-44 files, 247459 bytes uncompressed, 58836 bytes compressed:  76.2%
+Zip file size: 70145 bytes, number of entries: 45
+-rw-r--r--  2.0 unx    40134 b- defN 23-Apr-25 14:28 opentf/commons/__init__.py
+-rw-r--r--  2.0 unx    14520 b- defN 23-Apr-25 14:28 opentf/commons/expressions.py
+-rw-r--r--  2.0 unx     5521 b- defN 23-Apr-25 14:28 opentf/commons/selectors.py
+-rw-r--r--  2.0 unx        0 b- defN 23-Apr-25 14:28 opentf/schemas/__init__.py
+-rw-rw-rw-  2.0 unx     2105 b- defN 23-Apr-25 14:28 opentf/schemas/abac.opentestfactory.org/v1alpha1/Policy.json
+-rw-rw-rw-  2.0 unx     1230 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/AgentRegistration.json
+-rw-rw-rw-  2.0 unx     1366 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/AllureCollectorOutput.json
+-rw-rw-rw-  2.0 unx     2174 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/EventBusConfig.json
+-rw-rw-rw-  2.0 unx     2702 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/ExecutionCommand.json
+-rw-rw-rw-  2.0 unx     1143 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/ExecutionError.json
+-rw-rw-rw-  2.0 unx     2226 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/ExecutionResult.json
+-rw-rw-rw-  2.0 unx     1356 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/GeneratorCommand.json
+-rw-rw-rw-  2.0 unx     6633 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/GeneratorResult.json
+-rw-rw-rw-  2.0 unx     2219 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/Notification.json
+-rw-rw-rw-  2.0 unx     2803 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/PluginMetadata.json
+-rw-rw-rw-  2.0 unx     2875 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/ProviderCommand.json
+-rw-rw-rw-  2.0 unx     3878 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/ProviderConfig.json
+-rw-rw-rw-  2.0 unx     4259 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/ProviderResult.json
+-rw-rw-rw-  2.0 unx     6301 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/SSHServiceConfig.json
+-rw-rw-rw-  2.0 unx     2835 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/ServiceConfig.json
+-rw-rw-rw-  2.0 unx     2415 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/Subscription.json
+-rw-rw-rw-  2.0 unx     9899 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/Workflow.json
+-rw-rw-rw-  2.0 unx     1149 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowCanceled.json
+-rw-rw-rw-  2.0 unx     1110 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowCompleted.json
+-rw-rw-rw-  2.0 unx     1264 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowResult.json
+-rw-rw-rw-  2.0 unx     2232 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha2/Notification.json
+-rw-rw-rw-  2.0 unx     6612 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1alpha2/SSHServiceConfig.json
+-rw-rw-rw-  2.0 unx     3492 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1beta1/ExecutionCommand.json
+-rw-rw-rw-  2.0 unx    10205 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1beta1/GeneratorResult.json
+-rw-rw-rw-  2.0 unx     4535 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1beta1/ProviderCommand.json
+-rw-rw-rw-  2.0 unx     4563 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1beta1/ProviderConfig.json
+-rw-rw-rw-  2.0 unx     5050 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1beta1/ProviderResult.json
+-rw-rw-rw-  2.0 unx     2850 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1beta1/ServiceConfig.json
+-rw-rw-rw-  2.0 unx    15369 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1beta1/Workflow.json
+-rw-rw-rw-  2.0 unx     3517 b- defN 23-Apr-25 14:28 opentf/schemas/opentestfactory.org/v1beta2/ServiceConfig.json
+-rw-rw-rw-  2.0 unx     1579 b- defN 23-Apr-25 14:28 opentf/scripts/launch_java_service.sh
+-rw-r--r--  2.0 unx    18703 b- defN 23-Apr-25 14:28 opentf/scripts/startup.py
+-rw-r--r--  2.0 unx    18132 b- defN 23-Apr-25 14:28 opentf/toolkit/__init__.py
+-rw-r--r--  2.0 unx    13130 b- defN 23-Apr-25 14:28 opentf/toolkit/channels.py
+-rw-r--r--  2.0 unx     7164 b- defN 23-Apr-25 14:28 opentf/toolkit/core.py
+-rw-rw-rw-  2.0 unx    11357 b- defN 23-Apr-25 14:28 opentf_toolkit-0.46.0.dist-info/LICENSE
+-rw-r--r--  2.0 unx     1875 b- defN 23-Apr-25 14:28 opentf_toolkit-0.46.0.dist-info/METADATA
+-rw-r--r--  2.0 unx       92 b- defN 23-Apr-25 14:28 opentf_toolkit-0.46.0.dist-info/WHEEL
+-rw-r--r--  2.0 unx        7 b- defN 23-Apr-25 14:28 opentf_toolkit-0.46.0.dist-info/top_level.txt
+-rw-rw-r--  2.0 unx     4905 b- defN 23-Apr-25 14:28 opentf_toolkit-0.46.0.dist-info/RECORD
+45 files, 257486 bytes uncompressed, 61915 bytes compressed:  76.0%
```

## zipnote {}

```diff
@@ -1,13 +1,16 @@
 Filename: opentf/commons/__init__.py
 Comment: 
 
 Filename: opentf/commons/expressions.py
 Comment: 
 
+Filename: opentf/commons/selectors.py
+Comment: 
+
 Filename: opentf/schemas/__init__.py
 Comment: 
 
 Filename: opentf/schemas/abac.opentestfactory.org/v1alpha1/Policy.json
 Comment: 
 
 Filename: opentf/schemas/opentestfactory.org/v1alpha1/AgentRegistration.json
@@ -111,23 +114,23 @@
 
 Filename: opentf/toolkit/channels.py
 Comment: 
 
 Filename: opentf/toolkit/core.py
 Comment: 
 
-Filename: opentf_toolkit-0.45.0.dist-info/LICENSE
+Filename: opentf_toolkit-0.46.0.dist-info/LICENSE
 Comment: 
 
-Filename: opentf_toolkit-0.45.0.dist-info/METADATA
+Filename: opentf_toolkit-0.46.0.dist-info/METADATA
 Comment: 
 
-Filename: opentf_toolkit-0.45.0.dist-info/WHEEL
+Filename: opentf_toolkit-0.46.0.dist-info/WHEEL
 Comment: 
 
-Filename: opentf_toolkit-0.45.0.dist-info/top_level.txt
+Filename: opentf_toolkit-0.46.0.dist-info/top_level.txt
 Comment: 
 
-Filename: opentf_toolkit-0.45.0.dist-info/RECORD
+Filename: opentf_toolkit-0.46.0.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## opentf/schemas/opentestfactory.org/v1alpha2/SSHServiceConfig.json

### Pretty-printed

 * *Similarity: 0.9990594317871042%*

 * *Differences: {"'properties'": "{'contexts': {'items': {'properties': {'context': {'properties': {'targets': "*

 * *                 "{'minItems': 0}}}}}}, 'pools': {'patternProperties': "*

 * *                 "{'^[a-zA-Z_][a-zA-Z0-9_-]+$': {'items': {'anyOf': {0: {'properties': {'tags': "*

 * *                 "{'items': {'pattern': '^[a-zA-Z][a-zA-Z0-9-]*$'}}}}}}, 'minItems': 0}}, "*

 * *                 "'minProperties': 0}}"}*

```diff
@@ -51,15 +51,15 @@
                                     }
                                 ]
                             },
                             "targets": {
                                 "items": {
                                     "type": "string"
                                 },
-                                "minItems": 1,
+                                "minItems": 0,
                                 "type": "array"
                             },
                             "trusted_authorities": {
                                 "items": {
                                     "type": "string"
                                 },
                                 "type": "array"
@@ -98,15 +98,15 @@
             "required": [
                 "name"
             ],
             "type": "object"
         },
         "pools": {
             "additionalProperties": false,
-            "minProperties": 1,
+            "minProperties": 0,
             "patternProperties": {
                 "^[a-zA-Z_][a-zA-Z0-9_-]+$": {
                     "items": {
                         "anyOf": [
                             {
                                 "additionalProperties": false,
                                 "properties": {
@@ -145,14 +145,15 @@
                                         "type": "string"
                                     },
                                     "ssh_host_keys": {
                                         "type": "string"
                                     },
                                     "tags": {
                                         "items": {
+                                            "pattern": "^[a-zA-Z][a-zA-Z0-9-]*$",
                                             "type": "string"
                                         },
                                         "minItems": 1,
                                         "type": "array"
                                     },
                                     "username": {
                                         "type": "string"
@@ -225,15 +226,15 @@
                                     "username",
                                     "tags"
                                 ],
                                 "type": "object"
                             }
                         ]
                     },
-                    "minItems": 1,
+                    "minItems": 0,
                     "type": "array"
                 }
             },
             "type": "object"
         }
     },
     "required": [
```

## opentf/scripts/startup.py

```diff
@@ -24,15 +24,14 @@
 import logging
 import os
 import subprocess
 import sys
 import tempfile
 import time
 
-import jwt
 import requests
 import yaml
 
 import opentf
 
 
 LOGGING_FORMAT = '[%(asctime)s] %(levelname)s in startup: %(message)s'
@@ -58,33 +57,27 @@
     'OPENTF_EVENTBUS_WARMUPDELAY': 2,
     'OPENTF_EVENTBUS_WARMUPURL': 'http://127.0.0.1:38368/subscriptions',
     'OPENTF_EVENTBUSCONFIG': 'conf/eventbus.yaml',
     'OPENTF_HEALTHCHECK_DELAY': 60,
     'OPENTF_LAUNCHERMANIFEST': 'squashtf.yaml',
     'OPENTF_PLUGINMANIFEST': 'plugin.yaml',
     'OPENTF_SERVICEMANIFEST': 'service.yaml',
-    'OPENTF_SSHCHANNELCONFIG': 'conf/sshee.yaml',
     'OPENTF_TRUSTEDKEYS_PATHS': None,
     'TRUSTEDKEYS_PATH': '/etc/squashtf',
     'TRUSTED_KEYS_FILE': 'trusted_key.pub',
     'KEY_SIZE': 4096,
     'PUBLIC_EXP': 65537,
     'PUBLIC_KEY': None,
-    'SSH_CHANNEL_HOST': None,
-    'SSH_CHANNEL_PASSWORD': None,
-    'SSH_CHANNEL_POOLS': None,
-    'SSH_CHANNEL_TAGS': None,
-    'SSH_CHANNEL_USER': None,
     'HTTP_PROXY': None,
     'HTTPS_PROXY': None,
     'NO_PROXY': None,
     'CURL_CA_BUNDLE': None,
     'OPENTF_BASE_URL': None,
 }
-ENVIRONMENT_SECRETS = {'SSH_CHANNEL_PASSWORD'}
+ENVIRONMENT_SECRETS = set()
 
 CA_END = '-----END CERTIFICATE-----'
 
 ########################################################################
 # Environment variables helpers
 
 
@@ -96,15 +89,15 @@
 def _get_env_int(var: str) -> int:
     """Read var from env, using default if not set or not int."""
     try:
         return int(_get_env(var))
     except ValueError:
         val = ENVIRONMENT_VARIABLES[var]
         logging.warning(
-            'Environment variable %s not an integer, defaulting to %d.',
+            "Environment variable '%s' not an integer, defaulting to %d.",
             var,
             val,
         )
         return val
 
 
 def dump_environment():
@@ -127,15 +120,14 @@
             else:
                 logging.info('  %s not set.  No default value.', var)
 
 
 OPENTF_MANIFEST = _get_env('OPENTF_LAUNCHERMANIFEST')
 PLUGIN_MANIFEST = _get_env('OPENTF_PLUGINMANIFEST')
 SERVICE_MANIFEST = _get_env('OPENTF_SERVICEMANIFEST')
-SSHCHANNEL_CONFIG = _get_env('OPENTF_SSHCHANNELCONFIG')
 EVENTBUS_CONFIG = _get_env('OPENTF_EVENTBUSCONFIG')
 
 HEALTHCHECK_DELAY = _get_env_int('OPENTF_HEALTHCHECK_DELAY')
 EVENTBUS_WARMUP_DELAY = _get_env_int('OPENTF_EVENTBUS_WARMUPDELAY')
 EVENTBUS_WARMUP_URL = _get_env('OPENTF_EVENTBUS_WARMUPURL')
 
 TRUSTEDKEYS_PATH = _get_env('TRUSTEDKEYS_PATH')
@@ -164,14 +156,16 @@
         return [path.replace(CORE, root) for root in sys.modules['opentf'].__path__]
     return [path]
 
 
 def _generate_token() -> Tuple[str, str]:
     """Generate temporary key and JWT token."""
     # pylint: disable=import-outside-toplevel
+    import jwt
+
     from cryptography.hazmat.primitives import serialization
     from cryptography.hazmat.primitives.asymmetric import rsa
     from cryptography.hazmat.backends import default_backend
 
     private_key = rsa.generate_private_key(
         public_exponent=PUBLIC_EXP, key_size=KEY_SIZE, backend=default_backend()
     )
@@ -219,24 +213,24 @@
 def parse_and_start(
     paths: List[str], item: str, disabled: Optional[List[str]] = None
 ) -> List[Any]:
     """Lookup item manifests and start them if not disabled."""
     result = []
     for path in paths:
         for entry in os.walk(path):
-            logging.debug('Reading path %s.', entry[0])
+            logging.debug("Reading path '%s'.", entry[0])
             if item not in entry[2]:
                 logging.debug('(No manifest found in path.)')
                 continue
-            logging.debug('(Found a %s manifest, parsing.)', item)
+            logging.debug("(Found a '%s' manifest, parsing.)", item)
             with open(os.path.join(entry[0], item), 'r', encoding='utf-8') as manifests:
                 for manifest in yaml.safe_load_all(manifests):
                     if disabled and manifest['metadata']['name'].lower() in disabled:
                         logging.debug(
-                            '(Plugin %s explicitly disabled, ignoring.)',
+                            "(Plugin '%s' explicitly disabled, ignoring.)",
                             manifest['metadata']['name'],
                         )
                         continue
                     if manifest.get('cmd') is None:
                         continue
                     SERVICES.add(manifest['metadata']['name'])
                     if manifest['cmd'] not in RUNNING:
@@ -319,40 +313,39 @@
 def maybe_generate_token() -> None:
     """Generate token if no trusted keys defined."""
     if not os.path.exists(TRUSTEDKEYS_PATH):
         os.makedirs(TRUSTEDKEYS_PATH)
     if public_key := os.environ.get('PUBLIC_KEY'):
         if len(public_key.split()) < 2:
             logging.error(
-                'PUBLIC_KEY must be of the form: type-name base64-encoded-ssh-public-key [optional comment], got: %s.',
+                "PUBLIC_KEY must be of the form: 'type-name base64-encoded-ssh-public-key [optional comment]', got: %s.",
                 public_key,
             )
             sys.exit(1)
         logging.debug('Using provided PUBLIC_KEY.')
         with open(
             os.path.join(TRUSTEDKEYS_PATH, TRUSTED_KEYS_FILE), 'w', encoding='utf-8'
         ) as key:
             key.write(public_key)
     if not os.listdir(TRUSTEDKEYS_PATH):
-        # generate a JWT token
-        token, pub = _generate_token()
         logging.info('Creating temporary JWT token')
+        token, pub = _generate_token()
         logging.info(token)
         with open(
             os.path.join(TRUSTEDKEYS_PATH, TRUSTED_KEYS_FILE), 'w', encoding='utf-8'
         ) as key:
             key.write(pub)
 
 
 def maybe_populate_keystore() -> None:
     """Populate Java keystore if CURL_CA_BUNDLE defined."""
     if (ca_bundle := os.environ.get('CURL_CA_BUNDLE')) is None:
         return
     if not os.path.isfile(ca_bundle):
-        logging.error('CURL_CA_BUNDLE (%s) does not exist, aborting.', ca_bundle)
+        logging.error("CURL_CA_BUNDLE '%s' does not exist, aborting.", ca_bundle)
         sys.exit(1)
 
     with open(ca_bundle, 'r') as bundle_file:
         ca_list = bundle_file.read().split(CA_END)
     if not ca_list[-1].rstrip():
         ca_list.pop()
 
@@ -377,15 +370,15 @@
     - ca: the certificate as a string
     """
     with tempfile.NamedTemporaryFile('w') as ca_file:
         ca_path = ca_file.name
         ca_alias = f'opentf:{ca_counter}_{os.path.basename(ca_path)}'
         try:
             ca_file.write(ca)
-            logging.debug('File %s written.', ca_path)
+            logging.debug("File '%s' written.", ca_path)
         except IOError as err:
             logging.error('An error occurred while writing the file: %s.', err)
             sys.exit(1)
         ca_file.flush()
         try:
             ca_import_execute = subprocess.run(
                 [
@@ -443,26 +436,26 @@
 
 
 def _ensure_abac_if_defined(name, value):
     if value:
         if not OPENTF_AUTHORIZATION_MODE:
             logging.error(
                 '{0} is defined but OPENTF_AUTHORIZATION_MODE is undefined.'
-                '  OPENTF_AUTHORIZATION_MODE must include "ABAC" to use {0}.'.format(
+                "  OPENTF_AUTHORIZATION_MODE must include 'ABAC' to use {0}.".format(
                     name
                 )
             )
             sys.exit(1)
         if 'ABAC' not in OPENTF_AUTHORIZATION_MODE.split(','):
             logging.error(
-                f'OPENTF_AUTHORIZATION_MODE must include "ABAC" to use {name}.'
+                "OPENTF_AUTHORIZATION_MODE must include 'ABAC' to use %s.", name
             )
             sys.exit(1)
         if not os.path.isfile(value):
-            logging.error(f'{name} ({value}) not found or not a file.')
+            logging.error('%s (%s) not found or not a file.', name, value)
             sys.exit(1)
 
 
 def check_environment():
     """Check environment consistency.
 
     Some variables are inter-dependent, so checking them ease
@@ -487,17 +480,15 @@
         core_services_image = True
     except:
         core_services_image = False
     logging.info(
         'OpenTestFactory python-toolkit version: %s', version('opentf-toolkit')
     )
     dump_environment()
-    logging.info(
-        'Reading OpenTestFactory Launcher Manifest \'%s\' ...', OPENTF_MANIFEST
-    )
+    logging.info("Reading OpenTestFactory Launcher Manifest '%s' ...", OPENTF_MANIFEST)
     try:
         with open(OPENTF_MANIFEST, 'r', encoding='utf-8') as manifest:
             conf = yaml.safe_load(manifest)
     except Exception as err:
         logging.error('Reading SquashTF manifest failed: %s.', str(err))
         sys.exit(1)
```

## opentf/toolkit/__init__.py

```diff
@@ -15,14 +15,17 @@
 """A toolkit for creating OpenTestFactory plugins."""
 
 from typing import Any, Callable, Dict, Optional
 
 import inspect
 import os
 import sys
+import time
+
+from collections import defaultdict
 
 from flask import request, g
 
 import yaml
 
 from opentf.commons import (
     make_app,
@@ -42,27 +45,55 @@
 
 ########################################################################
 
 SUBSCRIPTION_KEY = '__subscription uuid__'
 MANIFEST_KEY = '__manifest key__'
 KIND_KEY = '__kind key__'
 INPUTS_KEY = '__inputs key__'
+WATCHEDFILES_KEY = '__watched files__'
+
+WATCHDOG_POLLING_DELAY_SECONDS = 30
+WATCHDOG_POLLING_DELAY_KEY = 'watchdog_polling_delay_seconds'
 
 Handler = Callable[[Dict[str, Any]], Any]
 
+
 ########################################################################
+# Helpers for provider plugins
+
+
+def _normalize_inputs(inputs: Dict[str, Any]) -> None:
+    """Normalize inputs.
+
+    The 'normalized' form for inputs is with `-` separators, not `_`.
+
+    Non-normalized inputs are removed from the dictionary.
+
+    # Raised exceptions
+
+    A _core.ExecutionError_ is raised if an input is provided twice, in
+    a normalized as well as a non-normalized form.
+    """
+    for key in inputs.copy():
+        if '_' in key:
+            normalized = key.replace('_', '-')
+            if normalized in inputs:
+                raise core.ExecutionError(
+                    f"Both '{key}' and '{normalized}' specified in inputs."
+                )
+            inputs[normalized] = inputs.pop(key)
 
 
 def _ensure_inputs_match(
     plugin, labels: Dict[str, str], inputs: Dict[str, Any]
 ) -> None:
     """Check inputs.
 
-    Fills missing optional inputs with their default values if
-    specified.
+    Normalize inputs, fills missing optional inputs with their default
+    values if specified.
 
     # Raised exceptions
 
     A _core.ExecutionError_ is raised if a required entry is missing,
     or if an unexpected entry is found.
     """
     cache = plugin.config['CONTEXT'][INPUTS_KEY]
@@ -79,14 +110,19 @@
     ):
         if (entry := cache.get(keys)) is not None:
             break
     else:
         return
 
     declaration, additional_inputs = entry
+    for key in declaration:
+        if key.startswith('{'):
+            break
+    else:
+        _normalize_inputs(inputs)
     for key, definition in declaration.items():
         if key not in inputs:
             if definition.get('required'):
                 raise core.ExecutionError(f"Mandatory input '{key}' not provided.")
             if (default := definition.get('default')) is not None:
                 inputs[key] = default
 
@@ -97,62 +133,14 @@
         if key not in declaration and key.replace('_', '-') not in declaration:
             allowed = ', '.join(sorted([f"'{k}'" for k in declaration.keys()]))
             raise core.ExecutionError(
                 f"Unexpected input '{key}' found in function step.  Allowed inputs: {allowed}."
             )
 
 
-def _normalize_inputs(inputs: Dict[str, Any]) -> None:
-    """Normalize inputs.
-
-    The 'normalized' form for inputs is with `-` separators, not `_`.
-
-    Non-normalized inputs are removed from the dictionary.
-
-    # Raised exceptions
-
-    A _core.ExecutionError_ is raised if an input is provided twice, in
-    a normalized as well as a non-normalized form.
-    """
-    for key in inputs.copy():
-        if '_' in key:
-            normalized = key.replace('_', '-')
-            if normalized in inputs:
-                raise core.ExecutionError(
-                    f"Both '{key}' and '{normalized}' specified in inputs."
-                )
-            inputs[normalized] = inputs.pop(key)
-
-
-def _dispatch_providercommand(plugin, handler: Handler, body: Dict[str, Any]) -> None:
-    """Provider plugin dispatcher.
-
-    `handler` is expected to return either a list of steps or raise a
-    _core.ExecutionError_ exception.
-    """
-    try:
-        plugin.logger.debug('Calling provider function %s.', handler.__name__)
-        inputs: Dict[str, Any] = body['step'].get('with', {})
-        _normalize_inputs(inputs)
-        _ensure_inputs_match(plugin, body['metadata'].get('labels', {}), inputs)
-        core.publish_providerresult(handler(inputs))
-    except core.ExecutionError as err:
-        core.publish_error(str(err))
-    except Exception as err:
-        core.publish_error(f'Unexpected execution error: {err}.')
-
-
-def _dispatch_executioncommand(_, handler: Handler, body: Dict[str, Any]) -> None:
-    """Channel plugin dispatcher."""
-    try:
-        handler(body)
-    except Exception as err:
-        core.publish_error(f'Unexpected execution error: {err}.')
-
-
 def _get_target(
     labels: Dict[str, str], providers: Dict[str, Handler]
 ) -> Optional[Handler]:
     """Find target for labels.
 
     Finds the most specific provider.  Returns None if no provider
     matches.
@@ -177,71 +165,205 @@
                 parts.pop()
         if template in providers:
             return providers[template]
 
     return None
 
 
-def _read_hooks_definition(plugin, hooksfile: str, schema: str) -> None:
+INVALID_HOOKS_DEFINITION_TEMPLATE = {
+    'name': 'invalid-external-hooks-definition',
+    'events': [{'category': '_'}],
+    'before': [
+        {
+            'run': 'echo ::error::Invalid hooks definition.  Hooks defined by {name}_PROVIDER_HOOKS are disabled.  Please contact your orchestrator administrator for more info.',
+            'if': "runner.os == 'windows'",
+        },
+        {
+            'run': 'echo "::error::Invalid hooks definition.  Hooks defined by {name}_PROVIDER_HOOKS are disabled.  Please contact your orchestrator administrator for more info."',
+            'if': "runner.os != 'windows'",
+        },
+    ],
+}
+
+
+def _read_hooks_definition(
+    plugin, hooksfile: str, schema: str, invalid: Dict[str, Any]
+) -> None:
     """Read hooks definition file.
 
     Try to read hooks definition file and set or replace existing hooks
     with those of `hooksfile`.
 
     # Required parameters
 
     - plugin: a Flask object
     - hooksfile: a string, the hooks definition file
     - schema: a string, the schema name
+    - invalid: a dictionary, a hook definition
     """
+    config = plugin.config['CONFIG']
     try:
         with open(hooksfile, 'r', encoding='utf-8') as src:
             hooks = yaml.safe_load(src)
         if not isinstance(hooks, dict) or not 'hooks' in hooks:
             plugin.logger.error(
                 "Hooks definition file '%s' needs a 'hooks' entry, ignoring.", hooksfile
             )
+            config['hooks'] = [invalid]
             return
 
-        config = plugin.config['CONFIG']
-        if prev := config.get('hooks'):
+        if config.get('hooks'):
             plugin.logger.info("Replacing hooks definition using '%s'.", hooksfile)
         else:
             plugin.logger.info("Reading hooks definition from '%s'.", hooksfile)
 
         config['hooks'] = hooks['hooks']
         valid, extra = validate_schema(schema, config)
         if valid:
             return
 
         plugin.logger.error(
             "Error while verifying '%s' hooks definition: %s.", hooksfile, extra
         )
-        if prev:
-            config['hooks'] = prev
-        else:
-            try:
-                del config['hooks']
-            except KeyError:
-                pass
     except Exception as err:
         plugin.logger.error(
             "Error while reading '%s' hooks definition: %s.", hooksfile, err
         )
 
+    config['hooks'] = [invalid]
+
+
+########################################################################
+# Dispatchers
+
+
+def _dispatch_providercommand(plugin, handler: Handler, body: Dict[str, Any]) -> None:
+    """Provider plugin dispatcher.
+
+    `handler` is expected to return either a list of steps or raise a
+    _core.ExecutionError_ exception.
+    """
+    try:
+        plugin.logger.debug('Calling provider function %s.', handler.__name__)
+        inputs: Dict[str, Any] = body['step'].get('with', {})
+        _ensure_inputs_match(plugin, body['metadata'].get('labels', {}), inputs)
+        core.publish_providerresult(handler(inputs))
+    except core.ExecutionError as err:
+        core.publish_error(str(err))
+    except Exception as err:
+        core.publish_error(f'Unexpected execution error: {err}.')
+
 
-def watch_file(plugin, name: str, schema: str, handler) -> None:
-    """Read external definition if env define one."""
-    handler(plugin, name, schema)
+def _dispatch_executioncommand(_, handler: Handler, body: Dict[str, Any]):
+    """Channel plugin dispatcher."""
+    try:
+        return handler(body)
+    except Exception as err:
+        msg = f'Unexpected execution error: {err}.'
+        core.publish_error(msg)
+        return make_status_response('InternalError', msg)
+
+
+########################################################################
+#  Watchdog
+
+
+def _run_handlers(plugin, file, handlers):
+    """Run file handlers."""
+    for handler, args, kwargs in handlers:
+        try:
+            handler(plugin, file, *args, **kwargs)
+        except Exception as err:
+            plugin.logger.error(
+                "Handler '%s' for file '%s' failed: %s.  Ignoring.", handler, file, err
+            )
+
+
+def _watchdog(plugin, polling_delay):
+    """Watch changes and call handlers when appropriate."""
+    files_stat = defaultdict(float)
+    files_handlers = plugin.config[WATCHEDFILES_KEY]
+    while True:
+        for file in list(files_handlers):
+            try:
+                current_modified_time = os.stat(file).st_mtime
+            except OSError as err:
+                plugin.logger.debug("Could not stat file '%s': %s.", file, err)
+                current_modified_time = 0
+            if current_modified_time == files_stat[file]:
+                continue
+            plugin.logger.debug("Watched file '%s' has changed.", file)
+            files_stat[file] = current_modified_time
+            _run_handlers(plugin, file, list(files_handlers[file]))
+
+        time.sleep(polling_delay)
+
+
+def _start_watchdog(plugin) -> None:
+    """Set up a watchdog that monitors specified files for changes."""
+    import threading
+
+    try:
+        polling_delay = max(
+            WATCHDOG_POLLING_DELAY_SECONDS,
+            int(plugin.config['CONTEXT'].get(WATCHDOG_POLLING_DELAY_KEY, 0)),
+        )
+    except ValueError as err:
+        plugin.logger.error(
+            'Configuration parameter %s not an integer: %s.',
+            WATCHDOG_POLLING_DELAY_KEY,
+            str(err),
+        )
+        sys.exit(2)
+
+    plugin.logger.info('Watchdog configuration:')
+    plugin.logger.info(
+        '  polling delay in seconds (%s): %d', WATCHDOG_POLLING_DELAY_KEY, polling_delay
+    )
+    plugin.logger.debug('Starting configuration watchdog thread.')
+    threading.Thread(
+        target=_watchdog, args=(plugin, polling_delay), daemon=True
+    ).start()
+
+
+def watch_file(plugin, path: str, handler, *args, **kwargs) -> None:
+    """Watch file changes.
+
+    There can be more than one handler watching a given file.  A handler
+    is a function taking at least two parameters: a `plugin` object and
+    a file path (a string).  It may take additional parameters.  It will
+    be called whenever the file changes.
+
+    # Required parameters
+
+    - plugin: a Flask application
+    - path: a string, the file path
+    - handler: a function
+
+    # Optional parameters
+
+    - *args: an array
+    - **kwargs: a dictionary
+
+    The provided extra parameters, if any, are passed to the handler
+    whenever it is called.
+    """
+    need_init = plugin.config.get(WATCHEDFILES_KEY) is None
+    if need_init:
+        plugin.config[WATCHEDFILES_KEY] = defaultdict(list)
+    plugin.logger.debug("Adding configuration watcher for '%s'.", path)
+    plugin.config[WATCHEDFILES_KEY][path].append((handler, args, kwargs))
+    if need_init:
+        _start_watchdog(plugin)
 
 
 def _subscribe(
     plugin, cat_prefix: str, cat: str, cat_version: str, manifest: Dict[str, Any]
 ) -> str:
-    """Subscribe for the relevent event."""
+    """Subscribe for the relevant event."""
     context = plugin.config['CONTEXT']
     kind = context[KIND_KEY]
     labels = {}
     if cat is not None:
         labels['opentestfactory.org/category'] = cat
     if cat_prefix is not None:
         labels['opentestfactory.org/categoryPrefix'] = cat_prefix
@@ -262,30 +384,34 @@
 
     Spurious subscriptions may remain in case of brutal termination.
     """
     try:
         context = plugin.config['CONTEXT']
         context[SUBSCRIPTION_KEY] = []
         context[INPUTS_KEY] = {}
-        for manifest in context[MANIFEST_KEY]:
-            if 'action' not in manifest.get('metadata', {}):
-                continue
-            for event in manifest.get('events', []):
-                cat_prefix = event.get('categoryPrefix')
-                cat = event.get('category')
-                if cat or cat_prefix:
-                    cat_version = event.get('categoryVersion')
-                    context[SUBSCRIPTION_KEY].append(
-                        _subscribe(plugin, cat_prefix, cat, cat_version, manifest)
-                    )
-                else:
-                    plugin.logger.warning(
-                        "At least one of 'category', 'categoryPrefix' required, ignoring."
-                    )
-
+        if context[KIND_KEY] == PROVIDERCOMMAND:
+            for manifest in context[MANIFEST_KEY]:
+                if 'action' not in manifest.get('metadata', {}):
+                    continue
+                for event in manifest.get('events', []):
+                    cat_prefix = event.get('categoryPrefix')
+                    cat = event.get('category')
+                    if cat or cat_prefix:
+                        cat_version = event.get('categoryVersion')
+                        context[SUBSCRIPTION_KEY].append(
+                            _subscribe(plugin, cat_prefix, cat, cat_version, manifest)
+                        )
+                    else:
+                        plugin.logger.warning(
+                            "At least one of 'category', 'categoryPrefix' required, ignoring."
+                        )
+        elif context[KIND_KEY] == EXECUTIONCOMMAND:
+            context[SUBSCRIPTION_KEY].append(
+                subscribe(kind=EXECUTIONCOMMAND, target='inbox', app=plugin)
+            )
         run_app(plugin)
     finally:
         for subscription_id in plugin.config['CONTEXT'][SUBSCRIPTION_KEY]:
             unsubscribe(subscription_id, app=plugin)
 
 
 def make_plugin(
@@ -294,14 +420,15 @@
     channel: Optional[Handler] = None,
     generator: Optional[Handler] = None,
     provider: Optional[Handler] = None,
     providers: Optional[Dict[str, Handler]] = None,
     publisher: Optional[Handler] = None,
     manifest=None,
     schema=None,
+    configfile=None,
 ):
     """Create and return a new plugin service.
 
     One and only one of `channel`, `generator`, `provider`, `providers`,
     or `publisher` must be specified.
 
     If no `manifest` is specified, there must be `plugin.yaml` file in
@@ -318,16 +445,18 @@
     - name: a string
     - description: a string
     - `channel`, `generator` or `provider`: a function
     - providers: a dictionary
 
     # Optional parameters
 
-    - manifest: a dictionary or a list of dictionaries or None
-    - schema: a string or None
+    - manifest: a dictionary or a list of dictionaries or None (None by
+      default)
+    - schema: a string or None (None by default)
+    - configfile: a string or None (None by default)
 
     # Raised exceptions
 
     A _ValueError_ exception is raised if the provided parameters are
     invalid.
 
     # Returned value
@@ -356,43 +485,39 @@
             if target := _get_target(labels=labels, providers=providers):
                 _dispatch_providercommand(plugin, target, body)
             else:
                 plugin.logger.warning(f'Labels {labels} not handled by {name}.')
         elif provider:
             _dispatch_providercommand(plugin, provider, body)
         elif channel:
-            _dispatch_executioncommand(plugin, channel, body)
+            return _dispatch_executioncommand(plugin, channel, body)
         else:
             return make_status_response('BadRequest', 'Not implemented yet.')
 
         return make_status_response('OK', '')
 
     if len([_ for _ in (channel, generator, provider, providers, publisher) if _]) != 1:
         raise ValueError(
             "One and only one of 'channel', 'generator', 'provider', 'providers', or 'publisher' is required."
         )
-    if (
-        manifest is not None
-        and not isinstance(manifest, dict)
-        and not isinstance(manifest, list)
-    ):
+    if not (manifest is None or isinstance(manifest, (dict, list))):
         raise ValueError(
             "'manifest', if specified, must be a dictionary or a list of dictionaries."
         )
 
     kind = (
         EXECUTIONCOMMAND
         if channel
         else GENERATORCOMMAND
         if generator
         else PROVIDERCOMMAND
     )
     if not schema:
         schema = SERVICECONFIG if generator else PROVIDERCONFIG
-    configfile = f'conf/{name}.yaml'
+    configfile = configfile or f'conf/{name}.yaml'
     plugin = make_app(name, description, configfile=configfile, schema=schema)
     plugin.route('/inbox', methods=['POST'])(process_inbox)
 
     if manifest is None:
         manifestfile = 'plugin.yaml'
         try:
             manifestfile = os.path.join(
@@ -403,20 +528,23 @@
         except Exception as err:
             plugin.logger.error(
                 "Oops, could not get '%s' manifest, aborting: %s.", manifestfile, err
             )
             sys.exit(2)
 
     if kind == PROVIDERCOMMAND and os.environ.get(f'{name.upper()}_PROVIDER_HOOKS'):
-        # plugin.logger.info('Starting hooks definition watch thread.')
+        befores = INVALID_HOOKS_DEFINITION_TEMPLATE['before']
+        befores[0]['run'] = befores[0]['run'].replace('{name}', name.upper())
+        befores[1]['run'] = befores[1]['run'].replace('{name}', name.upper())
         watch_file(
             plugin,
             os.environ[f'{name.upper()}_PROVIDER_HOOKS'],
-            schema,
             _read_hooks_definition,
+            schema,
+            INVALID_HOOKS_DEFINITION_TEMPLATE,
         )
 
     plugin.config['CONTEXT'][MANIFEST_KEY] = manifest
     plugin.config['CONTEXT'][KIND_KEY] = kind
 
     core.register_defaultplugin(plugin)
```

## Comparing `opentf_toolkit-0.45.0.dist-info/LICENSE` & `opentf_toolkit-0.46.0.dist-info/LICENSE`

 * *Files identical despite different names*

## Comparing `opentf_toolkit-0.45.0.dist-info/METADATA` & `opentf_toolkit-0.46.0.dist-info/METADATA`

 * *Files 6% similar despite different names*

```diff
@@ -1,10 +1,10 @@
 Metadata-Version: 2.1
 Name: opentf-toolkit
-Version: 0.45.0
+Version: 0.46.0
 Summary: OpenTestFactory Orchestrator Toolkit
 Home-page: https://gitlab.com/opentestfactory/python-toolkit
 Author: Martin Lafaix
 Author-email: mlafaix@henix.com
 License: Apache Software License (https://www.apache.org/licenses/LICENSE-2.0)
 Platform: UNKNOWN
 Classifier: Development Status :: 4 - Beta
@@ -19,15 +19,14 @@
 Requires-Dist: PyJWT[crypto] (>=2.6)
 Requires-Dist: PyYAML (>=6)
 Requires-Dist: Flask (>=2.2.3)
 Requires-Dist: jsonschema (>=4.17)
 Requires-Dist: toposort (>=1.10)
 Requires-Dist: waitress (>=2.1.2)
 Requires-Dist: paste (>=3.5.2)
-Requires-Dist: watchdog (>=2.2.1)
 
 # opentf-toolkit
 
 This package is part of the OpenTestFactory initiative.
 
 ## Overview
```

## Comparing `opentf_toolkit-0.45.0.dist-info/RECORD` & `opentf_toolkit-0.46.0.dist-info/RECORD`

 * *Files 9% similar despite different names*

```diff
@@ -1,9 +1,10 @@
 opentf/commons/__init__.py,sha256=QcuzE_TVxLWbE_-SiiGw83pe-cYhpiXM_4JEg7XdyTI,40134
 opentf/commons/expressions.py,sha256=l4x_wH103HYZW2oDFhRnA5HWEX1ja9H4D0VXOnVQ63w,14520
+opentf/commons/selectors.py,sha256=yj7Os7_8osvOssKUxi8d4tpKSMKBAa5hM81TWKjIADI,5521
 opentf/schemas/__init__.py,sha256=47DEQpj8HBSa-_TImW-5JCeuQeRkm5NMpJWZG3hSuFU,0
 opentf/schemas/abac.opentestfactory.org/v1alpha1/Policy.json,sha256=JXsfNAPSEYggeyaDutSQBeG38o4Bmcr70dPLWWeqIh8,2105
 opentf/schemas/opentestfactory.org/v1alpha1/AgentRegistration.json,sha256=NQykqU-lKE8LtBhBiFUcpVJq00MRG6dZsoM1xedx6uQ,1230
 opentf/schemas/opentestfactory.org/v1alpha1/AllureCollectorOutput.json,sha256=-L9DDWA0A4x54bPMn4m6Qwi2tf2nHvzIPFOElTjaVck,1366
 opentf/schemas/opentestfactory.org/v1alpha1/EventBusConfig.json,sha256=2aIYtA07qRoauKNy-MH25Kzg1l1Q1fjY9_4YIvlyYgw,2174
 opentf/schemas/opentestfactory.org/v1alpha1/ExecutionCommand.json,sha256=Tsqy0imNqO6Qg6B_Yw-4OeNer3xpURv-ZDaYUoZ-l1c,2702
 opentf/schemas/opentestfactory.org/v1alpha1/ExecutionError.json,sha256=RG5kWowUNGEudkK9lJbMngkOtOV6fTdLzO-hx7ou0w0,1143
@@ -19,26 +20,26 @@
 opentf/schemas/opentestfactory.org/v1alpha1/ServiceConfig.json,sha256=hRhJj4CkvB6p0IKKLMIwyxnbTmJkyEiDJSUxqzv0exI,2835
 opentf/schemas/opentestfactory.org/v1alpha1/Subscription.json,sha256=zbehXTc0AGmfq1qBsrRO-3AfarNVeZ5gCv78nlWu1KQ,2415
 opentf/schemas/opentestfactory.org/v1alpha1/Workflow.json,sha256=PKuAGeyQnGjoYNbd3IUng4k8SIecIVelXzzOncewhyw,9899
 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowCanceled.json,sha256=4t3WwKoZ0eBY7Vh7JZX81-P4FBH7zMSaJ4BUVJZjcaA,1149
 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowCompleted.json,sha256=5wkdS2SabdMbxqdWsxSANwepQXzc8Whf_V0saIGPC8A,1110
 opentf/schemas/opentestfactory.org/v1alpha1/WorkflowResult.json,sha256=k6cfAyQBB6c-cSwkBOKbLSnzVyR9jwOmKJCtRHIDB28,1264
 opentf/schemas/opentestfactory.org/v1alpha2/Notification.json,sha256=vfIpC8HdGd7WfrFS9lWe4JAKSzy2jHG_PbgLVDhE2qo,2232
-opentf/schemas/opentestfactory.org/v1alpha2/SSHServiceConfig.json,sha256=Ey-UhaBYV7xz29XB7D_Y7cnYYFld7hkE0ShBoxyNHQM,6445
+opentf/schemas/opentestfactory.org/v1alpha2/SSHServiceConfig.json,sha256=kozLs-eXSjl78X-BF5hoy5yDS0l0gs2wtyA_pbFzWHU,6612
 opentf/schemas/opentestfactory.org/v1beta1/ExecutionCommand.json,sha256=6K9uYa150jqnejBMArJjM5QjmYDzlNdfMo1zioZNAu8,3492
 opentf/schemas/opentestfactory.org/v1beta1/GeneratorResult.json,sha256=O2K9Mm4yXygzecEfYFV3bKzfYzbLhMs2EyV4BumEQvk,10205
 opentf/schemas/opentestfactory.org/v1beta1/ProviderCommand.json,sha256=y797FuyVqJjVJ582VI5eUoien1u1XE4xXQnw93CDHzE,4535
 opentf/schemas/opentestfactory.org/v1beta1/ProviderConfig.json,sha256=FoA1gIGU1k1MJjptZxhJwRombO_s6zMZEOJ2WS-yMAA,4563
 opentf/schemas/opentestfactory.org/v1beta1/ProviderResult.json,sha256=2SEBAndGA3WxGjWFweBIqsgB32fxNlse4a9A8ReAn4U,5050
 opentf/schemas/opentestfactory.org/v1beta1/ServiceConfig.json,sha256=m5ZgWAKbutum7NAa4JtPuy5XbgQ4eH9DgXdoG2DOfnY,2850
 opentf/schemas/opentestfactory.org/v1beta1/Workflow.json,sha256=hIQTJXdSQoHLfLF1QrTrHEtTvHoWDgp9AJti9vaet_s,15369
 opentf/schemas/opentestfactory.org/v1beta2/ServiceConfig.json,sha256=rEvK2YWL5lG94_qYgR_GnLWNsaQhaQ-2kuZdWJr5NnY,3517
 opentf/scripts/launch_java_service.sh,sha256=FRYrQD704VWLGXQ3QI-M3m77olXexB0vVEwzyiKLkxc,1579
-opentf/scripts/startup.py,sha256=UBl-oszxB15ImUvL7yxBwm7s6hsHcOLk2tNR-WtReVg,19009
-opentf/toolkit/__init__.py,sha256=jztU1Uqb7UXE5P2bP5IGyc9hL_rmONtMOm1TRLRufDI,13537
+opentf/scripts/startup.py,sha256=46UL6uCBXId3z3009bIIOD-inFEKrgg3allNQQYFIZs,18703
+opentf/toolkit/__init__.py,sha256=e4eypGL0kSItkrNo0a3zCWMNku7me9uRI1oip866jRo,18132
 opentf/toolkit/channels.py,sha256=2Zjbakelz9UhL-JMWXHioYH55IE1Bix33VGAKuUY3Pw,13130
 opentf/toolkit/core.py,sha256=ABKXX-K8ErPgkFi4RD5YRP4iVYXl_Bv1QkTjgryE5GI,7164
-opentf_toolkit-0.45.0.dist-info/LICENSE,sha256=xx0jnfkXJvxRnG63LTGOxlggYnIysveWIZ6H3PNdCrQ,11357
-opentf_toolkit-0.45.0.dist-info/METADATA,sha256=RZJ90GPe-B0HeUZ0lk9SNR1RA9Wt_EruGBWV_3WkUaA,1909
-opentf_toolkit-0.45.0.dist-info/WHEEL,sha256=pkctZYzUS4AYVn6dJ-7367OJZivF2e8RA9b_ZBjif18,92
-opentf_toolkit-0.45.0.dist-info/top_level.txt,sha256=_gPuE6GTT6UNXy1DjtmQSfCcZb_qYA2vWmjg7a30AGk,7
-opentf_toolkit-0.45.0.dist-info/RECORD,,
+opentf_toolkit-0.46.0.dist-info/LICENSE,sha256=xx0jnfkXJvxRnG63LTGOxlggYnIysveWIZ6H3PNdCrQ,11357
+opentf_toolkit-0.46.0.dist-info/METADATA,sha256=bDOWMc4csKbIAeXGO5y8ukC2KgkcVUUCN7Zktv6Uo60,1875
+opentf_toolkit-0.46.0.dist-info/WHEEL,sha256=pkctZYzUS4AYVn6dJ-7367OJZivF2e8RA9b_ZBjif18,92
+opentf_toolkit-0.46.0.dist-info/top_level.txt,sha256=_gPuE6GTT6UNXy1DjtmQSfCcZb_qYA2vWmjg7a30AGk,7
+opentf_toolkit-0.46.0.dist-info/RECORD,,
```

